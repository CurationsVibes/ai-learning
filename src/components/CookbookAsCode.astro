---
interface Props {
  githubUrl?: string;
  repoPath?: string;
  hasLiveDemo?: boolean;
  demoUrl?: string;
}

const { githubUrl, repoPath, hasLiveDemo, demoUrl } = Astro.props;

// Generate GitHub URL if repo path provided
const fullGithubUrl = githubUrl || (repoPath ? `https://github.com/CurationsLA/ai-learning/tree/main/${repoPath}` : null);
const forkUrl = fullGithubUrl ? `${fullGithubUrl.split('/tree/')[0]}/fork` : null;
---

{fullGithubUrl && (
  <div class="cookbook-as-code">
    <div class="cac-header">
      <span class="cac-icon">💻</span>
      <h4>Cookbook as Code</h4>
    </div>
    <p class="cac-description">
      This cookbook includes runnable code examples. Fork, experiment, and build!
    </p>
    <div class="cac-buttons">
      <a href={fullGithubUrl} class="cac-button view" target="_blank" rel="noopener noreferrer">
        <span class="button-icon">
          <svg width="20" height="20" viewBox="0 0 16 16" fill="currentColor">
            <path d="M8 0C3.58 0 0 3.58 0 8c0 3.54 2.29 6.53 5.47 7.59.4.07.55-.17.55-.38 0-.19-.01-.82-.01-1.49-2.01.37-2.53-.49-2.69-.94-.09-.23-.48-.94-.82-1.13-.28-.15-.68-.52-.01-.53.63-.01 1.08.58 1.23.82.72 1.21 1.87.87 2.33.66.07-.52.28-.87.51-1.07-1.78-.2-3.64-.89-3.64-3.95 0-.87.31-1.59.82-2.15-.08-.2-.36-1.02.08-2.12 0 0 .67-.21 2.2.82.64-.18 1.32-.27 2-.27.68 0 1.36.09 2 .27 1.53-1.04 2.2-.82 2.2-.82.44 1.1.16 1.92.08 2.12.51.56.82 1.27.82 2.15 0 3.07-1.87 3.75-3.65 3.95.29.25.54.73.54 1.48 0 1.07-.01 1.93-.01 2.2 0 .21.15.46.55.38A8.013 8.013 0 0016 8c0-4.42-3.58-8-8-8z"></path>
          </svg>
        </span>
        <span>View on GitHub</span>
      </a>
      {forkUrl && (
        <a href={forkUrl} class="cac-button fork" target="_blank" rel="noopener noreferrer">
          <span class="button-icon">🍴</span>
          <span>Fork & Run</span>
        </a>
      )}
      {hasLiveDemo && demoUrl && (
        <a href={demoUrl} class="cac-button demo" target="_blank" rel="noopener noreferrer">
          <span class="button-icon">🚀</span>
          <span>Live Demo</span>
        </a>
      )}
    </div>
  </div>
)}

<style>
  .cookbook-as-code {
    background: linear-gradient(135deg, rgba(99, 102, 241, 0.1) 0%, rgba(139, 92, 246, 0.1) 100%);
    border: 1px solid rgba(99, 102, 241, 0.2);
    border-radius: 1rem;
    padding: 1.5rem;
    margin: 2rem 0;
  }

  .cac-header {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    margin-bottom: 0.75rem;
  }

  .cac-icon {
    font-size: 1.75rem;
    line-height: 1;
  }

  .cac-header h4 {
    margin: 0;
    font-size: 1.25rem;
    color: var(--sl-color-text);
  }

  .cac-description {
    margin: 0 0 1.25rem 0;
    opacity: 0.9;
    line-height: 1.6;
  }

  .cac-buttons {
    display: flex;
    gap: 0.75rem;
    flex-wrap: wrap;
  }

  .cac-button {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.625rem 1.25rem;
    background: rgba(255, 255, 255, 0.05);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 0.5rem;
    text-decoration: none;
    color: var(--sl-color-text);
    font-weight: 600;
    font-size: 0.9rem;
    transition: all 0.3s ease;
  }

  .cac-button:hover {
    transform: translateY(-2px);
    box-shadow: 0 8px 16px rgba(0, 0, 0, 0.15);
  }

  .cac-button.view {
    background: linear-gradient(135deg, #24292e 0%, #1a1e22 100%);
    color: white;
    border-color: transparent;
  }

  .cac-button.view:hover {
    background: linear-gradient(135deg, #2f363d 0%, #24292e 100%);
  }

  .cac-button.fork {
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    color: white;
    border-color: transparent;
  }

  .cac-button.demo {
    background: linear-gradient(135deg, #10b981 0%, #059669 100%);
    color: white;
    border-color: transparent;
  }

  .button-icon {
    display: inline-flex;
    align-items: center;
    font-size: 1rem;
    line-height: 1;
  }

  @media (max-width: 640px) {
    .cac-buttons {
      flex-direction: column;
    }

    .cac-button {
      width: 100%;
      justify-content: center;
    }
  }
</style>
